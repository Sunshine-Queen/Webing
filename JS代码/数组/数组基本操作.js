let dayOfWeek = ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday']; //用[]的形式创建数组,使用元素初始化数组
console.log(dayOfWeek.length); //数组长度
for (let i = 0; i < dayOfWeek.length; i++) {
    console.log(dayOfWeek[i]);
}